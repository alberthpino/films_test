/*
 * FavoritesPage Messages
 *
 * This contains all the text for the FavoritesPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FavoritesPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FavoritesPage container!',
  },
});
