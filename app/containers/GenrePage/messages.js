/*
 * GenrePage Messages
 *
 * This contains all the text for the GenrePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GenrePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GenrePage container!',
  },
});
