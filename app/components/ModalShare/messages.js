/*
 * ModalShare Messages
 *
 * This contains all the text for the ModalShare component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ModalShare';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ModalShare component!',
  },
});
