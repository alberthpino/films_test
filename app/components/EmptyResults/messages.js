/*
 * EmptyResults Messages
 *
 * This contains all the text for the EmptyResults component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EmptyResults';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EmptyResults component!',
  },
});
